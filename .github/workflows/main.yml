name: Build
on: [push]

jobs:
  build:
    name: build-docker
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v1

      - name: init-docker
        uses: docker://alpine:latest
      - name: step-1
        run: |
          echo "Hello, World?"
          touch test
      - name: step-2
        run: |
          ls -l
